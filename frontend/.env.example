# Discord API backend origin. Local development uses localhost.
# In production, pass the public backend origin (without /api or a trailing slash)
# as the VITE_DISCORD_API_URL Docker build argument, then rebuild the frontend.
VITE_DISCORD_API_URL=http://localhost:3001
